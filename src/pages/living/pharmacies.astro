---
import BaseLayout from '../../layouts/BaseLayout.astro';
import BusinessCard from '../../components/BusinessCard.astro';
import { getCollection } from 'astro:content';

const businesses = await getCollection('businesses');
const pharmacies = businesses
  .filter(b => b.data.subcategories.includes('pharmacy'))
  .sort((a, b) => a.data.neighbourhood.localeCompare(b.data.neighbourhood) || a.data.name.localeCompare(b.data.name));
---

<BaseLayout
  title="Pharmacies — Walton-on-Thames"
  description="Community pharmacies in Walton-on-Thames and Hersham, with addresses, phone numbers and opening hours."
>
  <slot name="structured-data">
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "BreadcrumbList",
      "itemListElement": [
        { "@type": "ListItem", "position": 1, "name": "Home", "item": "https://walton-on-thames.org/" },
        { "@type": "ListItem", "position": 2, "name": "Living", "item": "https://walton-on-thames.org/living/" },
        { "@type": "ListItem", "position": 3, "name": "Pharmacies", "item": "https://walton-on-thames.org/living/pharmacies/" }
      ]
    })} />
  </slot>

  <div class="page-hero">
    <div class="container">
      <nav aria-label="Breadcrumb">
        <ol class="breadcrumbs">
          <li><a href="/">Home</a></li>
          <li><a href="/living/">Living</a></li>
          <li aria-current="page">Pharmacies</li>
        </ol>
      </nav>
      <h1>Pharmacies</h1>
      <p>Community pharmacies in Walton-on-Thames and Hersham offering NHS prescription dispensing.</p>
    </div>
  </div>

  <section class="section">
    <div class="container">
      <p class="section-intro">No pharmacy with a Whiteley Village address was found — the pharmacies below are the nearest options. Hours shown are pharmacy-counter hours and can change, particularly around bank holidays — confirm before a special journey, or use <a href="https://www.nhs.uk/service-search/pharmacy/find-a-pharmacy" target="_blank" rel="noopener noreferrer">NHS's Find a Pharmacy service ↗</a>.</p>
      <div class="card-grid card-grid-3">
        {pharmacies.map(biz => (
          <BusinessCard
            name={biz.data.name}
            slug={biz.data.slug}
            category={biz.data.category}
            neighbourhood={biz.data.neighbourhood}
            description={biz.data.description}
            images={biz.data.images}
            featured={biz.data.featured}
          />
        ))}
      </div>
    </div>
  </section>

  <section class="section section-alt">
    <div class="container">
      <div class="info-card">
        <h2>Related local services</h2>
        <ul class="related-links">
          <li><a href="/living/gp-surgeries/">GP Surgeries</a></li>
          <li><a href="/living/dentists/">Dentists</a></li>
          <li><a href="/living/post-offices-and-post-boxes/">Post Offices & Post Boxes</a></li>
          <li><a href="/living/council-and-services/">Council & Services</a></li>
        </ul>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .section-intro {
    color: var(--color-slate);
    max-width: 70ch;
    margin-bottom: 1.5rem;
    line-height: 1.6;
  }

  .section-alt {
    background: var(--color-cream, #f7f4ee);
  }

  .info-card {
    background: var(--color-white);
    border-radius: var(--radius-lg);
    padding: 1.75rem;
    box-shadow: var(--shadow-card);
    max-width: 60ch;
  }

  .info-card h2 {
    font-size: 1.05rem;
    margin-bottom: 1rem;
  }

  .related-links {
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 1.5rem;
  }

  .related-links a {
    font-size: 0.9rem;
    color: var(--color-navy);
  }
</style>
